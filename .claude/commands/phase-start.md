Start a new implementation phase for LOOM.

Steps:
1. Check auto-memory at `C:\Users\Adrian\.claude\projects\D--Proj-LOOM\memory\` for prior context and phase progress.
2. Read `docs/loom-implementation-plan.md` to identify the current phase.
3. Read all PRD documents referenced by the current phase section.
4. Check for an existing plan file in `C:\Users\Adrian\.claude\plans\` — if one exists, read it. If not, enter plan mode to create one.
5. Verify the previous phase's testable checkpoints still pass (run `tsc --noEmit` and `cargo check` at minimum).
6. Create a task list for the current phase deliverables.
7. Begin implementation.
