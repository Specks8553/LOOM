[package]
name = "loom_app"
version = "2.0.0-dev"
description = "Local-first, encrypted, AI-assisted creative writing"
edition = "2021"
rust-version = "1.77"
publish = false

[lib]
name = "loom_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# Tauri
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"

# Serde / errors
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

# Async runtime + cancellation
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync"] }
tokio-util = "0.7"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

# DB
rusqlite = { version = "0.32", features = ["sqlcipher", "bundled"] }

# Crypto / utility
zeroize = "1"
uuid = { version = "1", features = ["v4"] }

# Type generation (SB-3)
ts-rs = { version = "10", features = ["serde-compat", "no-serde-warnings"] }

[dev-dependencies]
tempfile = "3"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
